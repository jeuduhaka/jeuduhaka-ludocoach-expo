import React from 'react';

// const enabled = false;

// if (enabled && process.env.NODE_ENV !== 'production') {
//   //fix for Cannot set property createClass of #<Object> which has only a getter
//   //https://github.com/garbles/why-did-you-update/issues/45
//   // eslint-disable-next-line no-unused-vars,react/no-deprecated
//   let createClass = React.createClass;
//   Object.defineProperty(React, 'createClass', {
//     set: nextCreateClass => {
//       createClass = nextCreateClass;
//     },
//   });
//   //endfix

//   const { whyDidYouUpdate } = require('why-did-you-update');
//   whyDidYouUpdate(React, {
//     //By default, the changes for each component are grouped by component and these groups collapsed
//     groupByComponent: true,
//     collapseComponentGroups: true,
//     //include or exclude components by their displayName
//     // include: /^pure/,
//     exclude: /^View/,
//   });
// }

export default React;
